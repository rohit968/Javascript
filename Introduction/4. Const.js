// Declaring a constant using CONST keyword.

const name = "Rohit"

console.log(name);

//constants are immutable i.e. the values cannot be changed.

//EX: name = "Tiwari" will give the error as "Assignment to a constant variable"